# مسنجر — نمونه فایل تنظیمات محیط
# این فایل را کپی کنید به .env و مقادیر را پر کنید.
# فایل .env را در گیت کامیت نکنید.

# کلید مخفی فلاسک (جلسات و کوکی‌ها). در پروداکشن حتماً مقدار قوی و تصادفی بگذارید.
SECRET_KEY=change-this-in-production-secret-key-12345

# کلید رمزنگاری پیام‌ها و فایل‌ها (Fernet، رشته base64).
# اگر خالی بگذارید، برنامه از فایل fernet.key استفاده می‌کند یا یکی می‌سازد.
# برای ساخت کلید: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# FERNET_KEY=

# محیط اجرا. برای پروداکشن (HTTPS و کوکی امن) روی production بگذارید.
# FLASK_ENV=development
# FLASK_ENV=production
